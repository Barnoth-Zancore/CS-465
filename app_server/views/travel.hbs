{{> header}}

<div id="contents">
    <div class="box">
        <div>
            <div class="body">

                <h1>Travel</h1>

                {{#if message}}
                    <p>{{message}}</p>
                {{/if}}

                <ul id="sites">

                    {{#each trips}}

                    <li>

                        <a href="/api/trips/{{code}}">
                            <img src="{{image}}" alt="Img">
                        </a>

                        <h2>
                            <a href="/api/trips/{{code}}">{{name}}</a>
                        </h2>

                        <p>
                            {{description1}}
                        </p>

                        <p>
                            {{description2}}
                        </p>

                    </li>

                    {{/each}}

                </ul>

            </div>
        </div>
    </div>
</div>

{{> footer}}